/*Задача 2. 
Вивести на екран номери місяців весни і літа (від 3 до 8)
*/

// виведення номерів місяців весни і літа
for (let month = 3; month <= 8; month++) {
  month < 6
    ? document.write(`<p>Номер місяця весни: ${month}</p>`)
    : document.write(`<p>Номер місяця літа: ${month}</p>`);
}
